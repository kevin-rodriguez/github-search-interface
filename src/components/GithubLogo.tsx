import { Box } from '@mui/material';
import GithubLogoIcon from '../assets/icons/GithubLogoIcon';

const GithubLogo = () => {
	return (
		<Box sx={{ marginTop: 2, marginBottom: 4, textAlign: 'center' }}>
			<GithubLogoIcon />
		</Box>
	);
};

export default GithubLogo;
